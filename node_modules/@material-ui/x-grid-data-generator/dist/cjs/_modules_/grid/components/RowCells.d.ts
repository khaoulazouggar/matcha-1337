import * as React from 'react';
import { Columns, RowModel, CellIndexCoordinates } from '../models';
interface RowCellsProps {
    columns: Columns;
    domIndex: number;
    extendRowFullWidth: boolean;
    firstColIdx: number;
    hasScroll: {
        y: boolean;
        x: boolean;
    };
    lastColIdx: number;
    row: RowModel;
    rowIndex: number;
    scrollSize: number;
    showCellRightBorder: boolean;
    cellFocus: CellIndexCoordinates | null;
}
export declare const RowCells: React.FC<RowCellsProps>;
export {};
