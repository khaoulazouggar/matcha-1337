import { ColDef } from '../../models/colDef/index';
import { GridOptions } from '../../models/gridOptions';
import { SortDirection } from '../../models/sortModel';
interface ColumnHeaderItemProps {
    colIndex: number;
    column: ColDef;
    isDragging: boolean;
    isResizing: boolean;
    sortDirection: SortDirection;
    sortIndex?: number;
    options: GridOptions;
    filterItemsCounter?: number;
}
export declare const ColumnHeaderItem: ({ column, colIndex, isDragging, isResizing, sortDirection, sortIndex, options, filterItemsCounter, }: ColumnHeaderItemProps) => JSX.Element;
export {};
