import { ApiRef } from '../../../models/api/apiRef';
import { GridState } from './gridState';
export declare const useGridState: (apiRef: ApiRef) => [GridState, (stateUpdaterFn: (oldState: GridState) => GridState) => void, () => void];
