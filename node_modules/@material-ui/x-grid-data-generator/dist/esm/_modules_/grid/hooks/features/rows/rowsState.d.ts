import { RowId, RowModel } from '../../../models/rows';
export interface InternalRowsState {
    idRowsLookup: Record<RowId, RowModel>;
    allRows: RowId[];
    totalRowCount: number;
}
export declare const getInitialRowState: () => InternalRowsState;
