import { Density } from '../../../models/density';
import { GridState } from '../core/gridState';
import { DensityState } from './densityState';
export declare const densitySelector: (state: GridState) => DensityState;
export declare const densityValueSelector: import("reselect").OutputSelector<GridState, Density, (res: DensityState) => Density>;
export declare const densityRowHeightSelector: import("reselect").OutputSelector<GridState, number, (res: DensityState) => number>;
export declare const densityHeaderHeightSelector: import("reselect").OutputSelector<GridState, number, (res: DensityState) => number>;
