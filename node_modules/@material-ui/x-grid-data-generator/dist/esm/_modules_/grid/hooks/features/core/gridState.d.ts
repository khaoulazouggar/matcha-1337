import { InternalColumns } from '../../../models/colDef/colDef';
import { ContainerProps, ScrollBarState, ViewportSizeState } from '../../../models/containerProps';
import { GridOptions } from '../../../models/gridOptions';
import { ColumnMenuState } from '../columnMenu/columnMenuState';
import { ColumnReorderState } from '../columnReorder/columnReorderState';
import { DensityState } from '../density/densityState';
import { FilterModelState } from '../filter/FilterModelState';
import { VisibleRowsState } from '../filter/visibleRowsState';
import { KeyboardState } from '../keyboard/keyboardState';
import { PaginationState } from '../pagination/paginationReducer';
import { PreferencePanelState } from '../preferencesPanel/preferencePanelState';
import { InternalRowsState } from '../rows/rowsState';
import { SelectionState } from '../selection/selectionState';
import { SortingState } from '../sorting/sortingState';
import { InternalRenderingState } from '../virtualization/renderingState';
export interface GridState {
    rows: InternalRowsState;
    pagination: PaginationState;
    options: GridOptions;
    isScrolling: boolean;
    columns: InternalColumns;
    columnReorder: ColumnReorderState;
    columnMenu: ColumnMenuState;
    rendering: InternalRenderingState;
    containerSizes: ContainerProps | null;
    viewportSizes: ViewportSizeState;
    scrollBar: ScrollBarState;
    sorting: SortingState;
    keyboard: KeyboardState;
    selection: SelectionState;
    filter: FilterModelState;
    visibleRows: VisibleRowsState;
    preferencePanel: PreferencePanelState;
    density: DensityState;
}
export declare const getInitialState: () => GridState;
