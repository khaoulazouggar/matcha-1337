import { RowId, RowModel } from '../../../models/rows';
import { GridState } from '../core/gridState';
import { InternalRowsState } from './rowsState';
export declare type RowsLookup = Record<RowId, RowModel>;
export declare const rowsStateSelector: (state: GridState) => InternalRowsState;
export declare const rowCountSelector: import("reselect").OutputSelector<GridState, number, (res: InternalRowsState) => number>;
export declare const rowsLookupSelector: import("reselect").OutputSelector<GridState, Record<RowId, RowModel>, (res: InternalRowsState) => Record<RowId, RowModel>>;
export declare const unorderedRowModelsSelector: import("reselect").OutputSelector<GridState, RowModel[], (res: InternalRowsState) => RowModel[]>;
